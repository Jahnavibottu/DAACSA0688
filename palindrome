#include<stdio.h>
#include<string.h>
int main()
{
	int length,i;
	char s[100];
	printf("enter the value:");
	scanf("%s",s);
	length=strlen(s);
	for(i=0;i<length;i++)
	{
		if(s[i]!=s[length-i-1])
		printf("%s is not palindrome",s);
		else
		printf("%s is palindrome",s);
		break;
	}
}
